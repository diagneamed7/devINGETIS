<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <h1>categories</h1>
    <ul>
        <!-- <% categories.forEach(category => { %>
            <li><%= category.name %> - $<%= product.price %> (<%= product.category ? product.category.name : 'category %>)</li>
        <% }) %> -->
    </ul>
</body>
</html>